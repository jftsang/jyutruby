{
  "name": "jyutruby",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx parcel build src/index.html",
    "dev": "npx parcel watch src/index.html",
    "clean": "rm -r dist"
  },
  "targets": {
    "default": {
      "publicUrl": "./"
    }
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "hyperapp": "^2.0.0",
    "sass": "^1.101.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "parcel": "^2.16.4"
  }
}
